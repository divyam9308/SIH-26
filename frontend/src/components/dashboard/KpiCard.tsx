import type { DashboardKPI } from '../../types/dashboard'; import { Card } from '../ui/Card'; export function KpiCard({item}:{item:DashboardKPI}){return <Card className="p-4"><p className="text-[10px] font-bold tracking-wide text-slate-500">{item.title}</p><p className="mt-2 text-2xl font-bold text-slate-800">{item.value}</p><p className={`mt-2 text-xs font-medium ${item.tone==='blue'?'text-emerald-600':'text-red-500'}`}>{item.change}</p></Card>}
