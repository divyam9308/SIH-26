import { ArrowRight, Bell, Info } from 'lucide-react'; import { Area, AreaChart, CartesianGrid, LabelList, ResponsiveContainer, XAxis, YAxis } from 'recharts';
import type { EarlyWarningAlert, EarlyWarningTimeline } from '../../types/earlyWarnings';
const formatDate = (value: string) => new Intl.DateTimeFormat('en-IN', { day: 'numeric', month: 'short', year: 'numeric' }).format(new Date(`${value}T00:00:00`));
export function EscalationTimeline({ timeline }: { timeline?: EarlyWarningTimeline | null }) { if (!timeline) return <section className="rounded-xl border border-border bg-card p-5 shadow-[0_1px_2px_rgba(16,24,40,0.05)]"><div className="flex items-center gap-2"><h2 className="text-lg font-semibold text-foreground">Escalation Timeline</h2><Info className="size-4 text-muted-foreground" /></div><p className="mt-8 text-sm text-muted-foreground">No project is available for the selected filters.</p></section>; const change = timeline.risk_change; return <section className="rounded-xl border border-border bg-card p-5 shadow-[0_1px_2px_rgba(16,24,40,0.05)]"><div className="flex items-center gap-2"><h2 className="text-lg font-semibold text-foreground">Escalation Timeline</h2><Info className="size-4 text-muted-foreground" /></div><p className="mt-2 text-sm font-medium text-primary">{timeline.project_name}</p><div className="mt-3 flex flex-col gap-4 sm:flex-row"><div className="min-w-0 flex-1"><p className="text-xs text-muted-foreground">Model risk severity (ordinal 0–100)</p><div className="h-[180px]"><ResponsiveContainer width="100%" height="100%"><AreaChart data={timeline.points} margin={{ top: 18, right: 12, left: -18, bottom: 0 }}><defs><linearGradient id="riskFill" x1="0" y1="0" x2="0" y2="1"><stop offset="0%" stopColor="var(--critical)" stopOpacity={.22} /><stop offset="100%" stopColor="var(--critical)" stopOpacity={.02} /></linearGradient></defs><CartesianGrid stroke="var(--border)" vertical={false} /><XAxis dataKey="month" tickLine={false} axisLine={false} tick={{ fontSize: 11, fill: 'var(--muted-foreground)' }} /><YAxis domain={[0, 100]} ticks={[0, 50, 100]} tickLine={false} axisLine={false} tick={{ fontSize: 11, fill: 'var(--muted-foreground)' }} /><Area type="linear" dataKey="score" stroke="var(--critical)" strokeWidth={2} fill="url(#riskFill)" isAnimationActive={false} dot={{ r: 4, fill: 'var(--critical)', strokeWidth: 0 }}><LabelList dataKey="score" position="top" offset={10} style={{ fontSize: 12, fontWeight: 600, fill: 'var(--foreground)' }} /></Area></AreaChart></ResponsiveContainer></div></div><div className="w-full shrink-0 self-center rounded-lg border border-critical/20 bg-critical-soft p-4 text-center sm:w-44"><p className="text-xs text-muted-foreground">Risk Change (prior snapshot)</p><p className="mt-1 text-2xl font-bold text-critical">{change === null ? '—' : `${change > 0 ? '+' : ''}${change} pts`}</p><div className="my-3 border-t border-critical/20" /><p className="text-xs text-muted-foreground">Current Risk</p><p className="mt-1 text-base font-semibold text-critical">{timeline.current_risk.score} ({timeline.current_risk.level})</p></div></div></section>; }
export function RecentAlerts({ alerts = [] }: { alerts?: EarlyWarningAlert[] }) { return <section className="rounded-xl border border-border bg-card p-5 shadow-[0_1px_2px_rgba(16,24,40,0.05)]"><div className="flex items-center gap-2"><h2 className="text-lg font-semibold text-foreground">Recently Triggered Alerts</h2><Info className="size-4 text-muted-foreground" /></div><div className="mt-1 flex justify-end"><button type="button" className="inline-flex items-center gap-1 text-sm font-medium text-primary hover:underline">View all alerts <ArrowRight className="size-3.5" /></button></div><ul className="mt-2 divide-y divide-border">{alerts.map(alert => <li key={`${alert.project_name}-${alert.date}`} className="flex items-start gap-3 py-3.5"><Bell className="mt-0.5 size-4 shrink-0 text-critical" /><p className="flex-1 text-sm text-muted-foreground"><span className="font-semibold text-foreground">{alert.project_name}</span> {alert.text}</p><span className="shrink-0 text-xs text-muted-foreground">{formatDate(alert.date)}</span></li>)}{alerts.length === 0 && <li className="py-6 text-center text-sm text-muted-foreground">No alert transitions match the selected filters.</li>}</ul></section>; }
