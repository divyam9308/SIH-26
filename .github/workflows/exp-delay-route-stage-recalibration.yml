name: Delay route-stage recalibration experiment
on:
  pull_request:
    branches: [main]
    paths:
      - 'backend/app/ml/experiments/post_exp113_component_common.py'
      - 'backend/app/ml/experiments/exp_delay_route_stage_recalibration.py'
      - 'tests/test_exp_delay_route_stage_recalibration.py'
      - '.github/workflows/exp-delay-route-stage-recalibration.yml'
  workflow_dispatch:

jobs:
  compare:
    name: full 2001-2021 comparison
    runs-on: ubuntu-latest
    timeout-minutes: 360
    env:
      PYTHONUNBUFFERED: '1'
      OMP_NUM_THREADS: '2'
      OPENBLAS_NUM_THREADS: '2'
      MKL_NUM_THREADS: '2'
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: pip
      - run: pip install -r requirements-dev.txt
      - name: Focused tests
        run: pytest -q tests/test_exp_delay_route_stage_recalibration.py
      - name: Full leakage-safe comparison
        env:
          PYTHONPATH: '${{ github.workspace }}'
        run: python -m backend.app.ml.experiments.exp_delay_route_stage_recalibration --output audit_outputs/exp_delay_route_stage_recalibration.json
      - name: Show result
        if: always()
        run: test ! -f audit_outputs/exp_delay_route_stage_recalibration.json || cat audit_outputs/exp_delay_route_stage_recalibration.json
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: exp-delay-route-stage-recalibration
          path: audit_outputs/exp_delay_route_stage_recalibration.json
          if-no-files-found: warn
