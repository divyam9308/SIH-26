name: Experiment J Cost comparison

on:
  pull_request:
    branches: [main]
    paths:
      - 'backend/app/ml/experiments/experiment_j_cost_tender_economics.py'
      - 'data/external/official_project_tender_economics.csv'
      - 'tests/test_experiment_j_cost_tender_economics.py'
      - '.github/workflows/experiment-j-cost.yml'
  workflow_dispatch:

jobs:
  compare:
    runs-on: ubuntu-24.04
    timeout-minutes: 360
    env:
      PYTHONPATH: ${{ github.workspace }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: pip
      - run: pip install -r requirements-dev.txt
      - name: Focused tests
        run: |
          python -m py_compile backend/app/ml/experiments/experiment_j_cost_tender_economics.py
          pytest -q tests/test_experiment_j_cost_tender_economics.py
      - name: Run primary comparison or honest evidence-gap control
        run: |
          mkdir -p audit_outputs
          python -m backend.app.ml.experiments.experiment_j_cost_tender_economics --end 2021 --output audit_outputs/experiment_j_2001_2021.json
      - name: Validate evidence
        run: |
          python - <<'PY'
          import json
          from pathlib import Path
          x=json.loads(Path('audit_outputs/experiment_j_2001_2021.json').read_text())
          assert x['experiment_id']=='exp_j'
          assert (x['comparison_test_projects'],x['comparison_test_snapshots'])==(721,11200)
          assert x['delay_predictions_identical'] is True
          assert x['holdout_used_for_selection'] is False
          assert x['execution_verdict']=='EXECUTION VALID'
          if not x['details'].get('verified_external_data_available'):
              assert abs(float(x['experiment_cost_mae'])-float(x['production_cost_mae'])) < 1e-12
              assert x['scientific_verdict']=='DO NOT PROMOTE'
          print(json.dumps(x,indent=2))
          PY
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: experiment-j-cost-2001-2021
          path: audit_outputs/experiment_j_2001_2021.json
          if-no-files-found: warn
