name: Experiment 42 comparison
on:
  pull_request:
    branches: [main]
    paths:
      - 'backend/app/ml/experiments/multihorizon_completion_exp42.py'
      - 'backend/app/ml/experiments/adapter_exp42.py'
      - 'tests/test_exp42_multihorizon_completion.py'
      - '.github/workflows/exp42-compare.yml'
  workflow_dispatch:

jobs:
  compare:
    runs-on: ubuntu-latest
    timeout-minutes: 300
    strategy:
      fail-fast: false
      matrix:
        include:
          - {start: 2001, end: 2019}
          - {start: 2001, end: 2021}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: {python-version: '3.12', cache: pip}
      - run: pip install -r requirements-dev.txt
      - run: PYTHONPATH=. pytest -q tests/test_exp42_multihorizon_completion.py
      - env: {PYTHONPATH: '${{ github.workspace }}'}
        run: >-
          python scripts/run_fast_current_experiment.py --experiment exp_42
          --start ${{ matrix.start }} --end ${{ matrix.end }} --test-end 2025
          --output audit_outputs/exp42_${{ matrix.start }}_${{ matrix.end }}.json
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: 'exp42-${{ matrix.start }}-${{ matrix.end }}'
          path: 'audit_outputs/exp42_${{ matrix.start }}_${{ matrix.end }}.json'
          if-no-files-found: warn
