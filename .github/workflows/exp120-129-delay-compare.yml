name: Exp120-129 Delay comparison
on:
  pull_request:
    branches: [main]
    paths:
      - 'backend/app/ml/experiments/exp12*_delay.py'
      - 'backend/app/ml/experiments/post_exp113_delay_common.py'
      - 'backend/app/ml/experiments/run_exp120_129.py'
      - 'backend/app/ml/production_exp105_exp113_fast.py'
      - '.github/workflows/exp120-129-delay-compare.yml'
  workflow_dispatch:

jobs:
  compare:
    if: startsWith(github.head_ref, 'experiment-12')
    name: compare (2021)
    runs-on: ubuntu-latest
    timeout-minutes: 360
    env:
      # Keep the exact six strict-forward production folds and the exact
      # canonical model/evaluation contract. Two outer cutoff fits still run
      # concurrently; each canonical fit may use up to three independent
      # internal OOF workers. This is orchestration-only and is intended to
      # avoid the six-hour hosted-runner timeout seen with the 2x2 layout.
      POST_EXP113_OOF_WORKERS: '2'
      POST_EXP113_THREADS_PER_WORKER: '1'
      SIH_FOLD_JOBS: '3'
      SIH_MODEL_THREADS: '1'
      PYTHONUNBUFFERED: '1'
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: pip
      - run: pip install -r requirements-dev.txt
      - name: Resolve experiment number
        shell: bash
        run: |
          EXP=$(echo "${GITHUB_HEAD_REF}" | sed -E 's/^experiment-([0-9]+).*/\1/')
          if ! [[ "$EXP" =~ ^12[0-9]$ ]]; then echo "Invalid experiment branch: ${GITHUB_HEAD_REF}"; exit 1; fi
          echo "EXP=$EXP" >> "$GITHUB_ENV"
      - name: Syntax check
        run: |
          python -m py_compile backend/app/ml/production_exp105_exp113_fast.py backend/app/ml/experiments/post_exp113_delay_common.py backend/app/ml/experiments/run_exp120_129.py backend/app/ml/experiments/exp${EXP}_delay.py
      - name: Fresh production-vs-experiment comparison
        env:
          PYTHONPATH: '${{ github.workspace }}'
        run: >-
          python -m backend.app.ml.experiments.run_exp120_129
          --exp ${EXP}
          --end 2021
          --output audit_outputs/exp${EXP}_2001_2021.json
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: exp${{ env.EXP }}-2001-2021
          path: audit_outputs/exp${{ env.EXP }}_2001_2021.json
          if-no-files-found: warn
